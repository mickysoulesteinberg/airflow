# ------------------------------------------------------------------------------
# Airflow + Docker Environment Configuration
# Copy this file to ".env" and replace placeholder values with your real secrets.
# Do NOT commit the real ".env" to GitHub — only keep this example file tracked.
# ------------------------------------------------------------------------------

AIRFLOW_IMAGE_NAME="apache/airflow:2.10.3"

# 🐳 Airflow Docker UID (used to avoid permission issues on your host machine)
# Run `id -u` in your terminal to find your UID if you forget.
AIRFLOW_UID=50000

# ------------------------------------------------------------------------------
# 🎵 Spotify API Credentials
# Generate at: https://developer.spotify.com/dashboard
# ------------------------------------------------------------------------------
SPOTIFY_CLIENT_ID=your-spotify-client-id-here
SPOTIFY_CLIENT_SECRET=your-spotify-client-secret-here

# ------------------------------------------------------------------------------
# ☁️ Google Cloud / BigQuery
# You’ll need a service account JSON key. Store the file locally and
# mount it into Docker with your docker-compose.yaml.
# ------------------------------------------------------------------------------
GCP_PROJECT_ID=your-gcp-project-id
GCP_DATASET=your_bigquery_dataset
GCP_TABLE=your_bigquery_table
GOOGLE_APPLICATION_CREDENTIALS=/opt/airflow/keys/your-service-account.json
# (Inside Docker: adjust mount path in docker-compose.yaml)

# ------------------------------------------------------------------------------
# ✉️ Email Configuration    
# ------------------------------------------------------------------------------
# Gmail IMAP connection for Airflow
AIRFLOW_CONN_MY_IMAP_CONN=imap://your-project-gmail@gmail.com:your-app-password@imap.gmail.com:993
GMAIL_USER=your-project-gmail
GMAIL_PASSWORD=your-app-password

